spring:
  application:
    name: so-sync

  datasource:
    url: jdbc:postgresql://localhost:5432/sosync
    username: postgres
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update

  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: lleilalieva@gmail.com
    password: finm jdif igza emny
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true